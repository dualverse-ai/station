# Sokoban Ray v3
RESEARCH_EVAL_MAX_PARALLEL_WORKERS: 4
RESEARCH_EVAL_TIMEOUT: 7200  # 2 hours for 4x30min parallel training

# Production config
RESEARCH_EVAL_USE_DIFF_GPU: false  # Ray handles GPU allocation