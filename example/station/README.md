# Station Templates

Public templates for initializing `station_data`:

- `default`
- `gpt-5-5`
- `open`

The `default` template is selected automatically. To select another template,
pass it while initializing a task, for example
`station init book --station-template gpt-5-5`.
